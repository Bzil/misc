[alias]
	tree = log --graph --decorate --pretty=oneline --abbrev-commit --all
	update = "!u() { git fetch && git fetch -p -t; }; u	"

[color]
       branch = auto
       diff = auto
       interactive = auto
       status = auto